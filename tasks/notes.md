# Notes

Running log of decisions, conventions, known fixes, and things to remember. Claude reads this at the start of every session.

---

## Code Conventions

**Language:** TypeScript (Node LTS >= 20). Monorepo via **npm workspaces** — `server/` (Node/`ws`, built with `tsc`, run with `tsx` in dev) and `web/` (React + Vite + Tailwind v3 + shadcn/ui).

**Build command:** `npm run build`  (→ `web/dist` via Vite, then `server/dist` via `tsc`)
**Test command:** `npm test`  (Vitest, unit + integration)
**Lint command:** `npm run lint`  (ESLint 9 flat config — `eslint.config.js`)

**Naming:**
- camelCase for functions/variables, PascalCase for types/React components
- test files: `*.test.ts` / `*.test.tsx` co-located next to source; integration tests under `server/test/integration/`; e2e specs under `e2e/`

**Patterns:**
- Immutable: return new objects, never mutate (heartbeat frames + WS client state are fresh each tick — see global coding rules)
- async/await, not callbacks
- Validate at boundaries (WS payloads parsed + shape-checked on the client; malformed frames dropped + logged, never thrown into the app)
- Small focused files (200–400 lines); comprehensive error handling (fail fast with clear messages, e.g. `EADDRINUSE`)
- TS is strict: `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax` (base config in `tsconfig.base.json`, both workspaces extend it)

---

## PRD Configuration

Where PRDs are stored. The `/prd` skill reads this at start to determine output mode.

```
prd_mode = file
```

Options:
- `file` — write `PRD.md` to the repo (default)
- `tracker` — publish as a tracker issue only
- `both-file-canonical` — file + tracker; file is canonical
- `both-tracker-canonical` — file + tracker; tracker is canonical

---

## Git Rules

- **Branch naming:** `implement/<issue-id>-<short-description>` (e.g. `implement/42-dark-mode`)
- **Commit format:** `#<issue-id> <description>` (e.g. `#42 Add dark mode toggle to settings`)
- **Never** commit directly to main — always use a branch + PR

---

## Test Commands

> Skills and agents read this section to run the correct test commands for your stack.
> Fill in every command that applies to your project. Leave others as `<!-- not applicable -->`.

**Level 1 — Build + Unit Tests (no external dependencies):**
- Build: `npm run build`  (also: `npm run typecheck` for `tsc -b` across both workspaces)
- Unit tests: `npm test`  (Vitest run — `server` node project + `web` jsdom project)

**Level 2 — Integration Tests (may require Docker/emulators):**
- Setup: not applicable — integration tests boot the real server in-process (no Docker/emulators)
- Integration tests: `npx vitest run server/test/integration`
- Cleanup: not applicable (in-process; server torn down in `afterAll`)

**Level 3 — Dev Server (for manual testing):**
- Dev server: `npm run dev`  (Vite + `tsx watch` for the server, run concurrently)
- Dev server URL: `http://127.0.0.1:5173`  (Vite; proxies `/ws` → Node server on `127.0.0.1:8787`)
- Prod (single process): `npm run build && npm start` → `http://127.0.0.1:8787`  (serves `web/dist` + WS on the same origin)
- e2e: `npx playwright test`  (drives the prod app; specs control their own server child process)

**Test filtering (for verify commands):**
- Run a specific file: `npx vitest run <path>`  (e.g. `npx vitest run server/src/heartbeat.test.ts`)
- Run a specific test: `npx vitest run <path> -t "<name>"`
- e2e: `npx playwright test`  (single spec: `npx playwright test e2e/heartbeat.spec.ts`)

---

## Worktree setup

- **Files to copy into a new worktree:** none yet — no gitignored config files are required to build (no `.env`/secrets in V1).
- **Restore command (run after creating a worktree):** `npm install`  (npm workspaces — one install resolves both `server/` and `web/`; this also rebuilds the `better-sqlite3` native addon in the fresh worktree).
- **Shared automatically (do NOT copy):** `tasks/`, the `.git` history.
- **SQLite DB — nothing to copy:** the thin-anchor store is generated at runtime in the default OS app-data dir (e.g. `~/Library/Application Support/DevOS/devos.db` on macOS; override with the `DEVOS_DB_PATH` env var) and is gitignored (`*.db` / `*.db-wal` / `*.db-shm`), so there is no DB file to carry into a new worktree.

---

## Known Fixes

<!-- Add entries when you discover something non-obvious that fixes a recurring problem. -->
<!-- | Date | Problem | Fix | -->
<!-- | 2026-04-10 | Docker build fails on M1 | Add `--platform linux/amd64` to docker build | -->

| Date | Problem | Fix |
| 2026-07-26 | An integration test that serves/reads a **build artifact** (`web/dist/index.html`) passed locally but 404'd in CI. CI runs `npm ci → lint → typecheck → test` with **no `npm run build`**, so `web/dist` doesn't exist on the runner; locally it lingered from a prior build → false green. | Make such tests **self-provision** the artifact: write a minimal fixture when the build output is absent and remove only what the test created (use the real build output as-is when present). Verify the CI condition locally by moving `web/dist` aside before running. Sibling of the PR #7 lesson ("run typecheck in the local gate, not just build+test"). |

---

## Decisions

<!-- Record why you chose approach A over approach B — future-you will thank present-you. -->
<!-- | Date | Decision | Why | -->
<!-- | 2026-04-08 | Use SQLite instead of Postgres for dev | Simpler local setup, no Docker needed | -->

| Date | Decision | Why |
| 2026-07-18 | V1 stack: Node/TS server, Agent SDK in-process (primary) + `claude -p` (fallback), React+Vite+Tailwind+shadcn/ui, WebSocket, SQLite via better-sqlite3 (raw, no ORM) | Locked in wayfinder ticket G; better-sqlite3 raw chosen — thin-anchor store is ~4 tiny tables, ORM is overkill |
| 2026-07-18 | PTY/tmux terminal-attach → deferred to post-V1 | Owned sessions run headless (no terminal exists); the UI transcript is the sole view. PTY only mirrors hand-opened foreign terminals — a side-case, not the main workflow (assign→watch→steer→gates→PR, all in-UI) |
| 2026-07-18 | **[VERIFIED]** Programmatic sessions authenticate via the Claude **subscription** (OAuth), NOT the pay-per-token API | Probed on this Mac with `ANTHROPIC_API_KEY` empty: both Agent SDK `query()` and `claude -p` ran successfully — both inherit the CLI's keychain OAuth login. Operating cost ≈ $0 metered; the real cap is plan **rate limits**, not budget. `total_cost_usd` from the stream is a usage indicator, not a bill. Multi-provider model routing (implies API keys) stays post-V1 per SPEC §9. Boundary: a headless host without the keychain login would need an API key (not a V1 scenario) |
| 2026-07-19 | Scaffold foundation: monorepo = **npm workspaces** (`server/` + `web/`); WS lib = **`ws`**; server binds **`127.0.0.1:8787`**; test tooling = **Vitest** (unit + integration) + **Playwright** (e2e). Origin-check / local WS token **deferred** to the M1 localhost-security task | server and web have genuinely different toolchains (Vite/React vs `tsc`/`tsx`) → workspaces give one install/lockfile with isolated configs. `ws` is lightweight and exposes the raw HTTP `upgrade` event the deferred security task needs. Loopback-only bind is the core V1 security boundary (ARCHITECTURE §6) |
| 2026-07-31 | **Pipeline consolidated: 5 role sessions → 2** (build: understand→plan→code→test→fix + report-only adversarial review); roles → roster-declared UI **phase labels** (rename anytime, data-only); **PR = single default human gate** (optional per-project plan-gate toggle; interrupts always break through); roster carries per-role **model+effort** (builder Opus-1M/medium, reviewer Opus-1M/high); context = per-wave checkpoint + respawn at ~80% of window (**never compact**, ~2 resumes → escalate); only BLOCK findings fail review (cap 3), Bridge opens the PR mechanically | Real usage data: 5× cold-start overhead per item with no quality gain vs the proven single-session `/implement` flow; upstream task sizing (description+AC) supplies the clean-context benefit; review independence is the one load-bearing separation. Grill 2026-07-31, 9 forks → grill-summary.md; compiled into SPEC §3.1 (rev 2026-07-31) + ARCH decision log (supersedes 2026-07-20 role-session entry) |
| 2026-07-19 | Project-lifecycle level (map `6h6gPPWFfHr5PwG8`, T1–T3) folded into docs — IN V1 | **T1** stage model = harness's New→Decide→Define→Build→Ship(→Learn); sticky high-water badge, `max(precedence)` detection, live-derived. **T2** "Kick off next stage" = launcher only (emergent advance), light gating, project-bound Team room (one component, two bindings). **T3** birth = shell out to harness installer ("New Idea"/"Add Project"), land at New, auto-launch `/wayfinder`. Compiled into ARCHITECTURE.md §9 + SPEC §4/§6/§9. Deferred fog: new-milestone reset, reliable Learn detection |

---

## In Progress

- ✅ [DEFINE] /architect — Development OS (V1) — 8 sections complete — output: docs/ARCHITECTURE.md
- ✅ [DECIDE] /grill-me — Lifecycle stage model & detection (wayfinder ticket 6h6gPRW84r82PG58) — 5 forks resolved, output: grill-summary.md
- ✅ [DECIDE] /grill-me — Kick off next stage — project-level advance (wayfinder ticket 6h6gPRg5GrgjqCQ8) — 6 forks resolved, output: resolution recorded on the ticket + map gist (map 6h6gPPWFfHr5PwG8)
- ✅ [DECIDE] /grill-me — New idea / project birth flow (wayfinder ticket 6h6gPRxpmhxGvqv8) — 5 forks resolved, output: resolution recorded on the ticket + map gist (map 6h6gPPWFfHr5PwG8)
- ✅ [DEFINE] compile — project-lifecycle map (6h6gPPWFfHr5PwG8) T1–T3 folded into docs/ARCHITECTURE.md §9 + docs/SPEC.md §4/§6/§9 (map destination reached)
- ✅ [DEFINE] /to-todoist — SPEC decomposed into build tasks: 4 milestones + 28 subtasks in Todoist "Build Mode" / section "DevOS Build" (M1 foundation → M2 session driving → M3 pipeline+gates → M4 lifecycle actions). First task: "Scaffold server + web shell + WebSocket" (M1, p1)
- ✅ [BUILD] /implement — SQLite thin-anchor store + Project Registry (M1, Todoist 6h6hMMG3PQh9vwM8) — merged PR #2. better-sqlite3 (raw) 4-table store; Registry persists only {path,pinned,displayName,uiPrefs}; pin/unpin over WS; 75 unit/integration + 3 e2e green. Deviation: better-sqlite3 pinned to 12.x (11.x has no Node-26 prebuild). Deferred: WS origin/token gate → M1 localhost-security task (first client-writable surface now exists)
- ✅ [BUILD] /implement — Project discovery + pin UI (M1, Todoist 6h6hMMP4J4XW6cWg) — merged PR #3. Depth-1 `.claude/` scanner over env `DEVOS_PROJECT_ROOTS` (default `~/Programming`); new WS `discover`→`candidates` frame pair (candidates are a live scan, NEVER persisted — thin-anchor held); ProjectPin upgraded → Discovered list + Pinned grid + empty state. 108 vitest + 4 e2e green; acceptance ACCEPTED; architect CLEAR; evaluator CLEAR. Hardening added in-PR: per-socket `discover` debounce + scanner entry-scan cap. **Deferred:** (1) WS origin/token gate — discovery escalated its impact (now leaks FS paths cross-origin, CSWSH); tracked as a comment on the "Projects Grid + localhost security" task (6h6hMMj3PX4Gjcr8), treat as BLOCK before end-user exposure. (2) Minor/skipped advisories NOT worth a task: client-side handling of the `registry:error` frame is still unmirrored in ws-client.ts (pre-existing since PR #2 — failed pin/unpin isn't surfaced to the user); redundant `entry.isSymbolicLink()` guard in scanner.ts (harmless).

- ✅ [BUILD] /implement — Git State Reader (M1, Todoist 6h6hMMRWgccMjjx8) — merged PR #7. Fourth live-derive reader: server `readGitState()` via `execFile` (single `git status --porcelain=v2 --branch`, zero-dep, offline, no fetch, 5s timeout) → frozen GitState, never cached; new per-path `git-state` WS frame pair (mirrors discover→candidates); web mirror + git-status line on pinned rows. **Fixed in-review:** flood-guard re-keyed per-path (was a per-socket scalar that dropped the fan-out); `useHeartbeat.test.ts` mock gap surfaced the **lesson: run typecheck in the local gate, not just build+test.** Hardened git invocation vs malicious repo-local `core.fsmonitor`/`core.hooksPath`. **Deferred:** path confinement → Origin/token task (6h6hMMj3PX4Gjcr8).
- ✅ [BUILD] /implement — Story + Project Lifecycle Readers (M1, Todoist 6h6hMMcgMvVRr6Mg) — merged PR #9. Sixth live-derive reader + client-composed lifecycle stage badge (New→Decide→Define→Build→Ship). Server `readLifecycleSignals` emits only the signals the client can't derive (local docs, started-story, feature-branch-with-commits, release-tag) over a new `lifecycle-signals` WS frame; the **stage is composed CLIENT-side** (`web/src/lib/lifecycle.ts` `resolveStage`, max(precedence)) reusing the card's already-fetched tracker-state — so the server no longer shells the tracker or runs `git status` for the lifecycle. `readStoryStates` flags a genuinely started story (executor-state.md + Progress section). Render-only `StageBadge` (data-stage) on each card. 230 vitest (24 files) + build + typecheck + lint green; acceptance ACCEPTED; AC-sanity: DevOS repo → Build (live). **Fixed in-review (4 agents):** (A) wayfinder:map mis-derived as Define — now Decide-only, never both; (B) **pinned-path access-control allowlist** (`isPinnedPath`) added to ALL THREE read frames (git-state/tracker-state/lifecycle-signals), fails closed — closes the arbitrary-path FS/git oracle; (C) Ship tightened to a release-tag regex, Build to a non-default branch with ≥1 commit; (D) read-duplication removed by moving stage derivation client-side. `createServer` now exposes `registry` as a test seam. **Deferred → 6h6hMMj3PX4Gjcr8:** WS Origin/token connection gate; unbounded flood-guard Maps + global bash-spawn cap; fully de-duplicating server-side git reads (needs a GitState contract extension for tags/commits).
- ✅ [BUILD] /implement — Projects Grid + localhost security (M1, Todoist 6h6hMMj3PX4Gjcr8) — merged PR #10. **The security task the readers all deferred.** WS connection gate closes the accumulated CSWSH / localhost-DNS-rebinding surface: `verifyClient` at the upgrade — Origin allowlist (present foreign origins rejected in BOTH dev+prod; origin-absent rejected in prod) + **prod-only local token** on the `Sec-WebSocket-Protocol` subprotocol (`['devos','token.<hex>']`, never a query param). Token minted per process (`randomBytes(32)`, in-memory, never persisted), delivered via `<meta name="devos-ws-token">` injected into prod `index.html`, `timingSafeEqual` compare, charset-validated (fails fast on non-hex). **HTTP-side twin:** `handleProd` rejects non-loopback `Host` (403) before serving the token-bearing index — closes the DNS-rebinding exfil. DoS hardening: bounded flood-guard Maps (evict at 256) + global bash-spawn semaphore (max 4, waiter queue capped 64). Projects Grid: each pinned project → a **card** (static mini-fleet placeholder + next-task strip + stage badge). 274 vitest (29 files) + 5 e2e green. **4 review agents:** acceptance ACCEPTED, architect CLEAR, evaluator CLEAR, security **BLOCK (1) + 3 ADVISORY — all fixed in-PR** (Host-header gate; dev-origins gated behind `!requireToken`; token charset validation; bounded spawn queue; silent-no-token warn). **CI hotfix:** `http-host.test.ts` served a build artifact CI never builds → self-provisions a fixture (see Known Fixes 2026-07-26). **Dev/prod split:** Origin enforced both modes; token prod-only (Node can't hand a token to the Vite dev page). **Deferred (non-blocking):** ARCHITECTURE.md §6/§5 doc update for token-delivery mechanism + spawn caps → follow-up doc pass; flood-guard eviction weakening under ≥256 active paths (bounded/safe, coverage test added).
- ✅ [BUILD] /implement — Tracker Adapter Gateway (read) (M1, Todoist 6h6hMMX6v4Jrm858) — merged PR #8. Fifth live-derive reader + next-task strip per card. Server `readTrackerState()` shells out to **each project's own** `.claude/trackers/active/get-sprint-issues.sh` (cwd=project root) via `execFile('bash',…)` — 5s timeout, 1 MiB cap, drop-don't-throw, frozen `TrackerState`, never cached; **quarantined** normalizer (`server/src/tracker/normalize.ts` — the ONE module with tracker logic; Todoist-only for V1, replaceable seam for a future normalized adapter op). New per-path `tracker-state` WS frame pair + per-path flood-guard (mirrors git-state); web mirror + `NextTaskLine` (task title / "no open tasks" / "no tracker" / "tracker unreachable"). 182 vitest (20 unit normalizer+reader, 3 WS integration incl. AC1/AC2 + single-socket fan-out) + build + typecheck green; acceptance ACCEPTED; architect CLEAR; security CLEAR (injection-safe — argv-form execFile, no shell); evaluator CLEAR. **Fixed in-review:** (A) normalizer rejects tasks missing id/content instead of coercing to `{id:"undefined"}`; (B) hand only an **allowlisted env** to the untrusted repo-local adapter (not full `process.env`) — verified live: real `td` still authenticates under the scrubbed env; (C) null-tracker renders distinct "no tracker" vs empty backlog. **Deferred → comment on 6h6hMMj3PX4Gjcr8:** unbounded per-socket flood-guard Maps + no global bash-spawn cap (memory-DoS); origin/token gate absence — tracker-state escalates its impact from "read git status" to "run a shell script" (BLOCK before non-trusted exposure).
- ✅ [BUILD] /implement — Session Manager (Agent SDK) (M2 — FIRST task, Todoist 6h6hMV2xg8MgXCG8) — merged PR #12. **M2 core: spawn + multiplex owned Agent-SDK sessions in the one process.** `session-engine.ts` isolates the `@anthropic-ai/claude-agent-sdk` (`query()` streaming) import behind an injectable `QueryFn`/`EngineSession` seam (real SDK default, deterministic fake in ALL CI tests — no live Claude in CI). `session-manager.ts`: live-session Map, detached stream-consume loop (running→ended/errored, `sdk_session_id` captured from `system/init`), per-session failure isolation, `stopAll` awaits consume loops on shutdown. `session-spawn-limit.ts` global semaphore (`MAX_CONCURRENT_SESSIONS=8`, queue 64, acquire→release). `session-store.ts` sole reader/writer of the `sessions` table + additive idempotent `role`-column migration (`ensureColumn` via `PRAGMA table_info`). New `session-spawn`→`session-state` WS frames; thin web mirror (spawn button + running-count, NO transcript). 307 vitest (1 skipped opt-in live probe) + 5 e2e green; acceptance ACCEPTED; architect CLEAR; evaluator CLEAR. **AC5 (real-SDK spawn under subscription auth, no API key) self-run PASS** via env-gated `DEVOS_LIVE_SDK=1` probe. **Security BLOCK ×2 fixed + re-validated in-PR:** (A) spawn cwd two-layer gate — pinned AND realpath-contained within a `PROJECT_ROOT` (`isWithinProjectRoots`, fails closed); pinning alone doesn't bound the subprocess cwd; (B) scrubbed subprocess env (allowlist mirroring `tracker-reader.buildAdapterEnv`, excludes `ANTHROPIC_API_KEY`/secrets) — live probe confirms subscription OAuth still works; (C) `workItemId` length bound; (D) acquire slot BEFORE DB insert (no unbounded row growth under flood). **Deferred (own downstream tasks):** Team room transcript, Steer+interrupt, Permission cards (`canUseTool`), Hook Event Bus, the Bridge orchestrator, per-socket spawn quota, "waiting — plan limit" first-class queued state, client-facing `session-error` frame, persisted-status crash-staleness reconcile, pin-time PROJECT_ROOTS containment.
- ✅ [BUILD] /implement (--autonomous) — Steer + interrupt (M2, Todoist 6h6hMV8QfFvwCMP8) — merged PR #16. **Two-way live session control.** `session-engine.ts` `kickoffInput` → queue-backed **`createInputStream`** (kickoff-first, stays-open, close-drains; bounded at `MAX_PENDING_INPUTS=256`); `EngineSession` seam gains `send(text)` alongside `interrupt()` (SDK-import stays sole to this file). `SessionManager.sendInput` (echoes a new **`user-text`** transcript event — the SDK doesn't echo streaming-input — then pushes to the engine, per-session isolated) + `interrupt(id)` (turn-abort only; session stays `running`). New inbound WS frames **`session-input`** (bounded `MAX_STEER_TEXT_LENGTH=8192`, rejected-not-truncated) + **`session-interrupt`**, routed fail-closed in `ws-gateway.ts` via `get(sessionId)→isPinnedPath` (mirrors `session-transcript-request`; no broadcast, no flood-guard — deliberate user action). Web: `ws-client` senders + `user-text` mirror; TeamRoom message input + Interrupt button + `user-text` row, threaded App→useProjects. 434 vitest (2 opt-in live probes skipped) + build + typecheck + lint green. **4 review agents:** acceptance ACCEPTED, evaluator/architect/security all CLEAR (0 BLOCK). **Fixed in-review:** bounded the `createInputStream` steer queue (security ADVISORY #1). **Deferred (ADVISORY, non-blocking):** no-op logs at error level (matches existing convention); transcript echo truncated to `MAX_TEXT_CHARS=4000` while full ≤8192 text reaches the engine (deliberate, consistent with assistant-text truncation). **Enables:** Permission cards (`canUseTool`) + `setPermissionMode` ride this same up-channel next.
- ✅ [BUILD] /implement (--autonomous) — Bridge orchestrator + role roster reader (M2/M3, Todoist 6h76V8J6cJxXJQV8) — merged PR #15. **The deterministic pipeline orchestrator (server code, NOT an agent — zero prompting/role logic).** `roster-reader.ts` reads+validates each project's `.claude/harness-roles.json` (drop-don't-throw → frozen typed `Roster`). `bridge.ts` walks the roster pipeline (navigator→shipwright→lookout→warden→harbormaster) via the existing `SessionManager`: gated-advance by default (pause at each gate), auto-advance when `ui_prefs.auto_advance` ON + clean end, generic `interrupt(path,{kind,reason})`→Needs-you-inbox breakthrough (pauses regardless of toggle), rework routing (respawn shipwright with an injected failure-report as `SpawnInput.prompt`) with a `reworkLoopCap`→escalate. **Thin-anchor HELD:** pipeline position is an in-memory `Map` only, never persisted; only `sessions.current_stage` (new `SpawnInput.currentStage` writer) + read-only `ui_prefs.auto_advance` touch storage. New `bridge-start`/`gate-approve`/`bridge-interrupt` inbound + `bridge-state` outbound WS frames; gateway routes them with `isPinnedPath`+`isWithinProjectRoots` fail-closed (mirrors `session-spawn`); `bridge-state` broadcast `isPinnedPath`-gated. Thin web mirror + `NeedsYouInbox` component. 397 vitest (1 skipped opt-in live probe) + build+typecheck+lint green. **4 review agents:** acceptance ACCEPTED, evaluator/architect/security all CLEAR (0 BLOCK). **Fixed in-review:** missing `run.paused` guard on the errored path (an interrupt must pause `errored` too, not just `ended` — AC3 hole) + regression test. **CI hotfix:** integration test hardcoded an absolute dev path to `.claude/harness-roles.json` → ENOENT on the runner; now self-provisions a minimal inline roster fixture (sibling of the 2026-07-26 self-provision lesson). **Scope split:** Permission/`agent_needs_input` interrupt sources DEFERRED to the tracked canUseTool + Hook Event Bus tasks (the generic breakthrough entry is built so they plug in with no rework). **Deferred (own follow-ups):** rework return-path semantics (stale `run.index` post-rework is an open pipeline-design decision AC4 left unspecified); hardening advisories — roster-read byte-cap, unbounded inbox cap, spawn-containment defense-in-depth/TOCTOU, rework-prompt provenance.
- ✅ [BUILD] /implement (--autonomous) — Permission cards (`canUseTool`) (M2, Todoist 6h6hMVH97RxgPmPg) — merged PR #17. **The human-in-the-loop security brake (ARCHITECTURE §6).** Wires the Agent SDK `canUseTool` callback from an owned headless session to the browser as an allow/deny card: engine `createPermissionBroker` (parks a Promise per `requestId`, bounded `MAX_PENDING_PERMISSIONS=256`, fail-closed on abort/teardown/over-cap, `denyAll` on iteration end) behind the sole-SDK-import `EngineSession` seam; `buildSessionOptions` sets `canUseTool` and **no `permissionMode`** (AC3: SDK default is NOT auto-approve). Manager relay: `onPermissionRequest` fan-out + `resolvePermission` (idempotent via a per-session `permissionToolNames` guard — pruned on resolve) + a `permission` audit transcript event (in-memory only, never persisted). New WS frames **`permission-request`** (outbound, `isPinnedPath`-gated broadcast) + **`permission-decision`** (inbound, `get(id)→isPinnedPath` fail-closed) — mirrors the PR #16 steer/interrupt up-channel. Web: `ws-client` + `useProjects` `pendingPermissions`/`resolvePermission`, TeamRoom + NeedsYouInbox allow/deny cards. No SQLite/schema changes. 467 vitest (2 opt-in live probes skipped) + build + typecheck + lint green; AC1–AC5 machine-green. **4 review agents:** acceptance ACCEPTED, evaluator/architect/security all CLEAR (0 BLOCK). **Fixed in-review:** manager-idempotency phantom-audit on unknown/stale/forged requestId (+2 tests); unbounded `permissionToolNames` growth (prune on resolve); abort-listener leak (`{ once: true }`); added 5 `ws-gateway.test.ts` permission-routing tests closing the `isPinnedPath` fail-closed coverage gap. **Deferred → M2 sub-tasks:** Bridge→App inbox wiring (App renders NeedsYouInbox with `bridgeState=null` placeholder — 6h8pM699qvfjw55g, p2), "always allow" per-session permission scope via SDK `updatedPermissions` (needs design decision — 6h8pM6GHrWr62w58, p3), ARCHITECTURE §6 realization doc note (6h8pM6PpwM9Pgq28, p4). Timeout policy settled: indefinite block (AC1 "pauses until answered"), no auto-deny timer in V1.

- ✅ [BUILD] /implement (--autonomous) — Session state + Fleet tab (M2, Todoist 6h6hMVRhWvgVrWW8) — the Agent Fleet view: a first-class left-rail peer tab showing, for every active owned session across pinned projects, a human-meaningful **derived state** (running/idle/blocked/waiting-on-rate-limit) + the current **phase persona** (Navigator/Shipwright/Lookout/Warden/Harbormaster), rendered as two never-flattened levels (work item → build/review sessions → inner subagents). Server gains a per-session **persona join** (`persona-reader.ts` — `phase.md` marker x `harness-roles.json` roster displayName, drop-don't-throw) over a new `session-personas` request/snapshot WS frame mirroring the 6 existing readers; `workItemId` (grouping key) + `rateLimited` (seam field, always `false` in prod — the live SDK plan-limit detector is a separate deferred M2 task) added to session identity. All other derived state composed **client-side** in a pure `fleet-state.ts` (`deriveFleet`/`deriveDerivedState`), reusing frames the client already has (§9.2 lifecycle-stage precedent); `useProjects` grows `sessionPersonas` (retain-previous fold) + `bridgeStates` (closes a deferred PR #17 follow-up). `App.tsx` grows a Projects/Fleet/Inbox left-rail tab shell. 589 vitest (2 opt-in probes skipped) + build+typecheck+lint green; integration (`fleet-personas.test.ts`, 4 scenarios) + e2e (`fleet.spec.ts`) green. **4 review agents:** acceptance ACCEPTED, architect CLEAR, evaluator CLEAR hard gates, security **BLOCK ×1 fixed + re-verified in-PR** — workItemId path-traversal into the persona-join filesystem read, closed with a boundary allowlist (`^[A-Za-z0-9_-]+$`) on `session-spawn`/`bridge-start` + a defense-in-depth guard in `persona-reader.ts` (+3 tests). **Fixed in-review (evaluator):** `deriveFleet` cross-project `workItemId` grouping collision → grouped by the composite `(projectPath, workItemId)` key (+1 test). **Deferred (non-blocking):** un-memoized `deriveFleet` in the `App` render (perf micro-opt, evaluator ADV); per-workItem `phase.md` read-dedupe (architect ADV, bounded ≤8 reads flood-guarded); `NeedsYouInbox` `bridgeState` wiring (pre-existing, tracked separately — Todoist 6h8pM699qvfjw55g); the live SDK-stream plan-limit **detector** itself stays a separate deferred M2 task — this story ships the seam + client-side derivation only, AC3 verified via an injected/fixture `rateLimited` signal. Ran autonomous — decisions log in the PR body.

---

- ✅ [BUILD] /implement (--autonomous) — Two-session consolidation: roster v2 + Bridge + phase markers + model/effort (M2/M3, Todoist 6h9f2c97Qv63FGqg) — merged PR #18. **DevOS server half of the 5→2 session pipeline consolidation (SPEC §3.1 rev 2026-07-31).** Ships together with the pre-approved harness half (roster v2 JSON `["builder","reviewer"]` w/ per-role `phases[]`/`model`/`effort`, `.claude/rules/phase-markers.md`, builder/reviewer agent defs, deleted 5 persona agents, SPEC/ARCH doc pass). Server code: `roster-reader` v2 (schemaVersion 2, drop-don't-throw, frozen); `roles.ts VALID_ROLES → [builder,reviewer]`; **model/effort passthrough** `SpawnParams→buildSessionOptions→query()` + `SpawnInput→spawn` (Effort union mirrors SDK EffortLevel, no import; `DEFAULT_MODEL='inherit'`); **Bridge** roster-derived rework target + loop cap 3 + reviewer BLOCK/CLEAR verdict branch + mechanical open-PR on CLEAR via an **injectable `OpenPrAdapter` seam** (argv-safe `execFile`, loud-fail under `codePlatform:none`→Needs-you inbox); verdict/report/PR-body read from the real `tasks/stories/<id>/` handoff (`evaluation.md` APPROVE/CHANGES REQUIRED, `pr-body.md`); `story-state-reader` parses `phase.md` defensively → `phase` on lifecycle snapshot + ws-protocol. 492 vitest (2 opt-in probes skipped) + build+typecheck+lint green; AC1–AC5 machine-green. **4 review agents:** acceptance ACCEPTED, security CLEAR, evaluator CLEAR hard gates; architect **BLOCK ×2 fixed + re-review CLOSED in-PR** — (1) roster model/effort never reached the spawn (Bridge discarded `roster.roles`) → threaded via `run.roles[role]` at the spawn choke point; (2) verdict signal wired to a fictional `.claude/review/*` → repointed to `tasks/stories/<id>/`, `builder.md` now names `pr-body.md` so both ends agree. Folded advisories: 256 KiB byte-cap on Bridge readers, `workItemId` on reviewer+rework spawns, accurate pr-adapter script-missing label. Absorbs Todoist 6h8jFj4Vv8JfpCc8 (rework return-path — closed). **Deferred (non-blocking):** roster `schemaVersion`/`roles.ts VALID_ROLES` remain a hand-mirrored contract (architect ADV#3); default `draftPrBody` verdict/advisory extraction is minimal (richer extraction left to an injected dep). Ran autonomous — decisions log in the PR body.

- ✅ [BUILD] /implement (--autonomous) — Hook Event Bus (M2, Todoist 6h6hMVP7PjXGj5Vg) — merged PR #19. **Observe foreign sessions the OS didn't spawn.** New loopback-only `POST /hooks` HTTP sink on the single `http.Server` (`server/src/hooks/hook-handler.ts` + `hook-bus.ts`, routed in `index.ts` before the static handler): a zero-dep forwarder (`scripts/hooks/forward-hook.mjs`) a foreign project registers as a Claude Code `command`-hook forwards Notification/SessionStart/SessionEnd JSON to it. `createHookBus` defensively parses (drop-not-throw, frozen), derives a foreign needs-you event on Notification (permission_prompt/idle_prompt/agent_needs_input) + clear on SessionEnd; ws-gateway broadcasts it into the existing `NeedsYouInbox` (mirrors `onPermissionRequest`), gated by the dual **isPinnedPath + realpath isWithinProjectRoots** fail-closed cwd check. New `foreign-session-needs-you` + `hook-bus-liveness` WS frames; `HookBusIndicator` surfaces "hook bus not connected" (ARCH §7); client `foreignNeedsYou` capped at 100. 532 vitest (2 opt-in probes skipped) + build+typecheck+lint green; e2e gate `server/test/integration/hook-bus.test.ts` green (7 scenarios AC1/AC2/SEC1-5). **4 review agents:** acceptance ACCEPTED, evaluator/architect/security all CLEAR (0 BLOCK). **Security spine — the one new inbound surface:** unauthenticated but loopback-only; a foreign process carries no browser Origin/token so the WS gate doesn't transfer → defenses are loopback bind + isLoopbackHost Host gate + **NEW Origin-header rejection (CSWSH twin of the WS gate, fixed in-review, SEC5)** + 64 KiB cap + uniform-204 no-oracle + server-stamped ts + reason control-char sanitize. **Fixed in-review:** SessionEnd `clear` now bypasses the realpath check (isPinnedPath only) so a stuck card clears even if the dir was deleted; checkStale `this`-fragility. Ran autonomous — decisions log in the PR body. **Deferred (non-blocking):** `/hooks` ingress rate-limit/token-bucket (loopback-only; repo's tracked flood-guard class, PR #8/#10); liveness flips before the cwd gate (indicator-only); body-read idle timeout; liveness emit chattiness; ARCH §6/ADR-0001 realization doc note; `DEVOS_LIVE` real-foreign-session probe (external hook-JSON contract has no CI oracle — parser degrades gracefully). **Harness rough edge hit:** worktree isolation forks from `main`, not the feature branch, so dependent waves couldn't see prior waves' files (Wave 2 agents self-merged); dropped isolation from Wave 4 on → candidate for /improve-harness.

- ✅ [BUILD] /implement (--autonomous → interactive) — Context-window map misses the real model: respawn at 160k not 800k (M2, Todoist 6h9wH4jjQ9fw9m3g) — merged PR #26. **The context-recycle window is now roster-declared, not guessed from the model id.** Root cause: `checkContextUsage` sized the window off the spawn-time `s.model`, which is the `'inherit'` placeholder for non-Bridge spawns; `windowFor('inherit')` missed `MODEL_CONTEXT_WINDOWS` → 200k default → 80% fired at 160k (5× early). Three-layer fix, roster-first: (1) **new optional per-role `contextWindow`** in `harness-roles.json` (both roles = 1_000_000), parsed by `roster-reader` (drop-don't-throw, additive — `schemaVersion` stays 2), threaded Bridge → `SpawnInput.contextWindow` → `LiveSession.declaredWindow` → the recycle check (a new model's window is now a one-line settings edit, SPEC §3.1); (2) **fallback** captures the SDK-resolved model from `system/init` (`resolvedModel`) and derives the window via a generalized `[Nm]` marker parse in `windowFor` (clamped ≤100M) — fixes the `'inherit'` path; (3) **loud-fail** warns once per session when neither a declared window nor a known model is available, else silently 200k. `context-watcher` refactored so `contextOccupancy`/`crossesThreshold` take an explicit window number; new `effectiveWindow(declaredWindow, model)` + `isKnownContextWindow`. 663 vitest (2 opt-in probes skipped) + typecheck + lint green. **4 review agents:** acceptance ACCEPTED, evaluator/architect/security all CLEAR (0 BLOCK). **Fixed in-review:** upper-bound cap on the declared window (mirrors the `[Nm]` ceiling so an absurd value can't disable the brake); control-char sanitize on the logged model id (mirrors `hook-bus.ts` sanitizeReason); emptied the now-redundant exact-id map; warn-once latch test + doc-comment nits; **SPEC §3.1 updated** ("model + effort + context window"). **Superseded + closed PR #24** (a `[1m]`-marker-only fix on the same Todoist task) — its marker-match (generalized to `[Nm]`) and warn-once idea folded in here; its branch deleted. **Process miss to learn from:** a plain-text `/implement` description skipped the live-tracker check (`rules/next-task.md`) that would have surfaced the already-open #24 — briefly duplicated it before catching it. Candidate for /improve-harness: `/implement <plain text>` should fuzzy-search open tracker tasks + open PRs for a title match before building. **Harness rough edge hit again:** a subagent ran `git checkout main` + `git pull` mid-run, landing the first commit on local `main` instead of the feature branch (recovered with no force-push, nothing lost) — subagents should not switch the working-tree branch.

- ✅ [BUILD] /implement (--autonomous) — "Always allow this tool" (per-session permission scope) (M2, Todoist 6h8pM6GHrWr62w58) — merged PR #30 (commit `f2d59b6`). **The click-fatigue relief for the PR #17 permission brake.** Adds a third "Always allow" choice to the permission cards: a new `'allow-always'` decision flows browser → `permission-decision` WS frame → gateway → `SessionManager.resolvePermission` → the engine broker's `resolve`, which returns `{ behavior:'allow', updatedPermissions:[{ type:'addRules', rules:[{ toolName }], behavior:'allow', destination:'session' }] }` instead of bare `{ behavior:'allow' }`, so the SDK stops re-prompting for that tool for the rest of THAT session only. **Not persisted, not global, NO `permissionMode` — AC3 (the runaway-agent brake) intact.** Server: broker retains `requestId→toolName` (pruned on resolve/abort/denyAll, mirrors `MAX_PENDING_PERMISSIONS`); `ws-protocol` widens the decision union + inbound validator; gateway + manager carry it through by type inference (no source edit — covered by tests). Web: `ws-client`/`useProjects` widen types; `NeedsYouInbox` + `TeamRoom` render the third button. 683 vitest (3 skipped incl. the env-gated AC5 live probe) + build + typecheck + lint green. **4 review agents:** acceptance ACCEPTED (AC1–6), architect CLEAR, security CLEAR, **evaluator BLOCK ×1 fixed** — the task `<verify>` (`build && vitest && lint`) missed the CI **`typecheck`** gate (ci.yml:31); 3 strict-null errors in the new test file, fixed test-only (`?.`/`?? {}`) + re-verified green. **Three design axes settled up front (the task's "design before build"):** (1) every gated tool eligible — the human's per-tool click IS the gate, no exclusion list; (2) whole-tool scope, no `ruleContent`; (3) no in-session revoke UX — revoke = end the session. Ran autonomous — decisions log in the PR body. **Deferred (non-blocking):** (A) prune `permissionToolNames` on broker abort/denyAll — spurious audit entry + per-session growth, pre-existing since PR #17, fix widens the `EngineSession.resolvePermission` seam → **Todoist 6hC5jxF4fVj23r5g**; (B) ARCHITECTURE §6 blast-radius doc note (builder can't edit docs in-PR) → **Todoist 6hC5jxPr79G9GHM8**. **Lesson candidate for /improve-harness:** task `<verify>` commands should run `npm run typecheck` (not just `build && vitest && lint`) so the CI type gate is caught locally — the test tsconfigs aren't type-checked by build/vitest.

- ✅ [BUILD] /implement (--autonomous) — Skills panel (view) (M3, Todoist 6h6hMg3VRxVF2mjg) — **the 7th live-derive reader.** Server `readSkills(projectPath)` recursively scans `.claude/skills/**/SKILL.md` (drop-don't-throw, frozen, never-cached), hand-parses `name`/`description` frontmatter (no yaml dep), and classifies each skill **org** vs **local** by matching its **manifest-relative path** against `.claude/.harness-manifest.json` `installedFiles`; streamed over a new per-path `skills` WS request/snapshot frame (mirrors `git-state`), gated by `isPinnedPath` **alone** (matches all 6 prior read-only reader frames). Thin `SkillsPanel` under a new `'skills'` left-rail tab renders org/local groups. Read-only V1, no new dependency. 705 vitest + typecheck + lint + `e2e/skills.spec.ts` green; AC1–AC5 machine-green. **4 review agents:** acceptance ACCEPTED, architect CLEAR, security CLEAR, **evaluator CLEAR hard gates + 2 CONFIRMED correctness blockers fixed in-run** — (#1) classification keyed on the frontmatter `name` not the manifest path → `ralph-prd` (frontmatter `name: prd`), an installed org skill, mis-rendered **local** on real DevOS data; (#2) top-level-only scan silently dropped the 7 nested `skills/frontend-design/*/SKILL.md` org skills. Both fixed by path-based classification + recursive **bounded** (`MAX_DEPTH=8`/`MAX_SKILLS=500`, symlinked-dir skip) SKILL.md discovery; verified live (39/39 skills, all org, no phantom container) + 2 regression tests. **Deferred (ship test = No):** per-file byte cap + SKILL.md symlink guard → Todoist 6hC7gfcqVWQhXx7g; ARCHITECTURE.md §1/§4 register the 7th reader + fail-closed-label note → Todoist 6hC7gfJ476JMRvWg. **Harness rough edge hit hard:** a CONCURRENT `/implement` session building the sibling M3 task 6h6hMfRFR7gwFrG8 (Work-item Detail) was live-editing this SAME working directory and switched the branch out from under this run mid-build, commingling both features' uncommitted work in shared files (ws-client.ts/ws-protocol.ts) — paused per autonomous pause-anyway (contradiction + irreversible), the user isolated the sibling work into a separate worktree, then this run reclaimed the primary dir (restore-to-HEAD + remove foreign untracked, all preserved in the sibling's commit) and re-ran the affected tasks. Strong `/improve-harness` signal: two `/implement` runs must never share one working directory (git-worktrees rule) — the orchestrator should detect/refuse a second run in a dirty tree it doesn't own. Ran autonomous — decisions log in the PR body.

- ✅ [BUILD] (interactive follow-up, not `/implement`) — Context-window follow-ups: unknown-window warning → Needs-you inbox; guard failed context-recycle respawn (M2) — merged PR #27 (merge `b40ace2`). Two follow-ups from the PR #22/#26 context-window work, plus one documented non-fix. (1) **Unknown-window warning now reaches a human:** `checkContextUsage`'s once-per-session `console.warn` (fallback to the guessed 200k default — no roster window + unrecognized model) only hit server stdout; added a once-per-session `ContextConfigWarning` signal (mirrors `onContextUsage`/`onCostUsage`) that the Bridge turns into an **advisory** Needs-you inbox item — gated by the same `windowWarned` latch (can't spam) and it does NOT change the run's gate. (2) **A failed context-recycle respawn can no longer advance the pipeline onto half-finished work:** `spawnAndEmit` swallows a spawn failure (logs, never throws), leaving `run.currentSessionId` pointing at the OLD session — so `endAtBoundary(oldId)` let its later `ended` correlate to the run and `handleEnded` advanced with a non-escalated gate (the cap-hit path was safe only because it sets `gate='escalated'` first). The respawn path now detects the un-overwritten `currentSessionId` and escalates with that same terminal gate before the old session ends. (3) **Declared-but-wrong `contextWindow` deliberately NOT validated:** catching a roster declaring 1M for a 200k model needs a model→window table — the exact coupling SPEC §3.1 removed to make the roster the single source of truth. Ship test (`rules/deferrals.md`) answered against **real configured inputs = No** (`harness-roles.json` declares 1M for `claude-opus-4-8[1m]`, a genuine 1M model → correct), so it's defense-in-depth → **deferred Todoist 6hC5HMRGR59pg748** (labeled `deferred`, 3-part body); doc comment added on `effectiveWindow()`. 665 vitest (2 opt-in probes skipped) + typecheck + lint green. **No 4-agent review gate this run** — interactive follow-up, self-reviewed and merged directly (not an autonomous `/implement`). **Process notes:** the `/update-harness` change (installs `rules/deferrals.md` + builder/autonomous-mode/skill refs) rode alongside in the working tree → split into its own PR #28; **direct push to `main` is blocked by branch protection** (learned this run — route even chore/config through a PR, not a direct `main` push).

- ✅ [BUILD] /implement (--autonomous) — Needs-you inbox (M3, Todoist 6h6hMfqH2Fj3hhM8) — merged PR #39. **The cross-cutting capstone of SPEC §6: "see everything blocked at once."** Turns the three already-streaming blocked-item sources (permission requests, bridge inbox items, foreign-session-needs-you) into a UNIFIED inbox: a new pure client deriver `web/src/lib/needs-you.ts` (mirrors `fleet-state.ts` — React-free, immutable, **zero new server persistence**, thin-anchor held) merges every blocked item across ALL pinned projects into one `NeedsYouItem[]` sorted **longest-wait-first** (ascending by `waitSince`, `key.localeCompare` tie-break); `NeedsYouInbox` reworked from three ungrouped blocks → one merged sorted list (all `data-testid`/callbacks preserved); new `web/src/hooks/useNeedsYouToast.ts` fires a feature-detected browser `Notification` OS-toast on each newly-appeared block (skips the initial backlog, path-basename body); new `LeftRail` Inbox blocked-count badge; `App.tsx` memoized-derives once + drives toast + badge. **Blocker fixed as part of the story (not deferred):** permission requests carried NO timestamp → the wait-time sort couldn't rank them; added a **server-stamped `ts` at broker park-time** threaded engine→ws-protocol→gateway frame→client parser (finite-validated at the boundary). 838 vitest + 68 integration + e2e (`needs-you-inbox.spec.ts` cross-project sorted DOM order + badge; `needs-you-cards` regression) green. **4 review agents:** acceptance ACCEPTED, evaluator/architect/security all CLEAR (0 BLOCK). **Fixed in-run (advisories):** OS-toast body shortened to path basename (security A09 lock-screen info-disclosure); `useMemo` on the derived list (evaluator churn nit). **Deferred → Todoist 6hCHJv9w77XWc8X8:** ARCHITECTURE §7 observability-table doc note for the OS-toast/badge mechanism (builder can't edit docs in-PR). **Known flaky (unrelated, not this diff):** `server/src/session/phase-watcher.test.ts` fs-watch timing test trips under full-suite load, passes 3/3 in isolation → `/improve-harness` candidate. Ran autonomous — decisions log in the PR body.

- ✅ [BUILD] /implement (--autonomous) — Evidence tabs (M3, Todoist 6h6hMg5GJhvCxcG8) — merged PR #41 (merge `1acf8cb`). **The 8th live-derive reader + the SPEC §6 "proof of work" surface inside Work-item Detail.** Four tabs below the pipeline timeline: Files changed (git diff of the work item's branch), Test results (`tasks/stories/<id>/regression.log` → `evaluation.md`/`acceptance.md` fallback), PR summary (`pr-body.md`), and an Audit trail of the process artifacts in `tasks/stories/<id>/` each badged **Draft/Final** (Final iff `phase.md` phase===`shipping`, else Draft — derived, never stored). Server: new `server/src/evidence/evidence-reader.ts` (`readEvidence`, drop-don't-throw/frozen/256 KiB byte-cap, dual `^[A-Za-z0-9_-]+$` workItemId guard, fixed 10-file artifact allowlist) + new exported `readChangedFiles` in `git-state-reader.ts` (hardened `execFile` git, base origin/HEAD→main→master, porcelain fallback, 2000-file cap). New `evidence-request`→`evidence` WS frame pair mirroring `work-item-sessions-request` end-to-end: ws-protocol validator (reuses `isSafeWorkItemId`), ws-gateway handler (`isPinnedPath` fail-closed + per-`(path,workItemId)` flood-guard 200ms + env-gated `DEVOS_E2E_EVIDENCE_FIXTURE` reconciled to the fleet-fixture path), web `ws-client` parser/`onEvidence`/`requestEvidence`, `useProjects` fold keyed by workItemId. New `EvidenceTabs.tsx` mounted in `WorkItemDetail` (request-on-mount) + `App` wiring. 889 vitest + 3 integration (live WS round-trip: changed files, regression summary, PR body, Final badges, pinned-gate, unsafe-id reject) + e2e (`evidence-tabs.spec.ts`, 4 tabs + Draft/Final badges) green. **4 review agents:** acceptance ACCEPTED (5/5), evaluator/architect/security all CLEAR (0 BLOCK, 0 finding ≥75%). **Fixed in-run:** readonly→mutable cast at the EvidenceTabs boundary (architect ADV#3) — EvidenceTabs now uses readonly arrays, cast + unused import removed. **Deferred (ship test = No):** test-results whole-file vs results-section extract → Todoist 6hCJP2Fpw6H7rCX8; filesChanged diff base uses local main (under-reports in detached-HEAD/CI) → 6hCJP2q64fF2WMFg; ARCHITECTURE §4 derived-data list omits the 8th reader → 6hCJP3GM87c9WqMg. Ran autonomous — decisions log in the PR body.

---

## Deferred (follow-ups)

<!-- Tracked non-blocking items surfaced during builds. -->
- ✅ **Bridge → App inbox wiring** (surfaced by Permission cards PR, Todoist 6h6hMVH97RxgPmPg) — RESOLVED before the Needs-you inbox story (PR #39). The `bridgeState={null}` / `onApprove={()=>{}}` placeholders described here no longer exist: `App.tsx` already renders `NeedsYouInbox` with real values (`bridgeStates`/`pendingPermissions`/`foreignNeedsYou` from `useProjects`, real gate/question/escalation/permission handlers) — landed by the Question+Escalation cards work (PR #38). The Needs-you inbox story confirmed it done (Phase 1 brief) and did not re-implement it.
- **Doc note: OS-toast + inbox-badge mechanism in ARCHITECTURE §7** (surfaced by the Needs-you inbox PR #39 architect review, Todoist 6hCHJv9w77XWc8X8): §7's observability table doesn't yet record the two client-side mechanisms the inbox realized — the browser `Notification` OS-toast (`web/src/hooks/useNeedsYouToast.ts`) and the LeftRail Inbox blocked-count badge — as the concrete realization of SPEC §6's "unified Needs-you inbox + OS toast + status bar." Ship test = No (doc-completeness only, behavior is correct); builder can't edit architecture docs in-PR per `documentation.md`. Same shape as the canUseTool §6 doc-note follow-up (PR #31).
- ✅ **ARCHITECTURE §6 realization note for canUseTool** — DONE, merged PR #31 (Todoist 6h8pM6PpwM9Pgq28). Added an "Agent action approval (realized — PR #17)" subsection to `docs/ARCHITECTURE.md` §6 mirroring the WS-gate/session-spawn realization notes: broker (`createPermissionBroker`, Promise parked per requestId behind the `EngineSession` seam) + `MAX_PENDING_PERMISSIONS=256` fail-closed (over-cap/abort → deny, teardown → `denyAll`) + "no `permissionMode`, SDK default is not auto-approve" (the runaway-agent brake) + the `permission-request`/`permission-decision` fail-closed up-channel + in-memory-only audit event + indefinite-block timeout. Forward-pointer to the PR #30 standing allow; blast-radius note stays scoped to 6hC5jxPr79G9GHM8. Docs-only `/implement --autonomous`; architect review CLEAR (0 BLOCK, 2 ADVISORY — ADV#1 precision fixed in-run, ADV#2 non-issue). Other 3 reviewers + code test suite skipped as no-surface for a zero-code Markdown diff (logged in PR "Decisions made on your behalf").
- ✅ **"Always allow this tool" (per-session permission scope)** — DONE, merged PR #30 (Todoist 6h8pM6GHrWr62w58). Shipped the `'allow-always'` decision → session-scoped `updatedPermissions` addRules; AC3 intact. Spawned two follow-ups (below).
- **Prune `permissionToolNames` on broker abort/denyAll** (surfaced by the "Always allow" PR #30 security review, Todoist 6hC5jxF4fVj23r5g): the manager-side `permissionToolNames` map is pruned only on `resolve`, not on the broker's `abort` (signal) or `denyAll` (teardown) paths, so a late/replayed `permission-decision` for an already-aborted requestId installs NO rule (broker no-op) yet the manager emits a spurious `kind:'permission'` audit event, and aborted requests leak un-pruned map entries until session teardown (per-session bounded). Pre-existing since PR #17. Non-blocking; a clean fix widens the `EngineSession.resolvePermission` seam (engine iface + real engine + manager + fakes) to report whether a request was actually pending — a security-seam change that warrants its own review.
- **ARCHITECTURE §6 — allow-always blast-radius note** (surfaced by the "Always allow" PR #30 architect review, Todoist 6hC5jxPr79G9GHM8): the allow-always rule is tool-wide (`{ toolName }`, no `ruleContent`), so "Always allow Bash" auto-approves every later Bash call for the session, including commands the human never saw. §6's "Agent action approval" row doesn't yet describe the per-tool session-scoped standing allow or its blast radius. One-line §6 note (builder can't modify architecture docs in-PR per `documentation.md`).
- **Sanity-check roster-declared `contextWindow` vs. the model** (surfaced by the context-window follow-up PR #27, Todoist 6hC5HMRGR59pg748): `effectiveWindow` accepts any finite/positive/≤`MAX_CONTEXT_WINDOW` declared window without checking it against the model, so a roster over-declaring (e.g. 1M for a model that holds 200k) recycles late or never. Not fixed in-run — catching it needs model knowledge (a model→window table) that SPEC §3.1 deliberately removed; real configured inputs are correct, so this is defense-in-depth against a future bad roster edit. Likely shape if revisited: roster-reader-side validation, keeping model knowledge out of the recycle path. See the deliberate-trust-boundary comment on `effectiveWindow()`.

- ✅ [DECIDE] /grill-me — Pipeline consolidation: 5 role sessions → 2 (build + adversarial review), roles become UI phase labels; usage-cost driven — 9 forks resolved, output: grill-summary.md

## Blockers

<!-- Things waiting on external action — APIs, people, services. -->
<!-- | What | Waiting on | Since | -->
<!-- | API v2 access | Third-party approval | 2026-04-05 | -->
